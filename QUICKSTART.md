# Quick Start Guide

## Быстрый старт для разработчика

### 1. Установка и запуск

```bash
# Клонируйте или откройте проект
cd d:\github_my\openapi-to-http

# Установите зависимости (уже выполнено)
npm install

# Скомпилируйте проект (уже выполнено)
npm run compile
```

### 2. Запуск расширения

1. Откройте проект в VS Code
2. Нажмите `F5` или выберите **Run > Start Debugging**
3. Откроется новое окно VS Code с активным расширением

### 3. Тестирование на примере

В новом окне VS Code:

```bash
# Откройте один из примеров:
File > Open File > examples/sample-api.yaml
# или
File > Open File > examples/ecommerce-api.json
```

### 4. Генерация .http файла

1. В открытом файле кликните **правой кнопкой мыши**
2. Выберите **"Generate .http"**
3. Введите имя файла, например: `my-test.http`
4. Нажмите Enter

### 5. Проверка результата

Расширение создаст `.http` файл с запросами. Откройте его и проверьте:

```http
# Generated from OpenAPI Specification
# Title: Sample Pet Store API
# Version: 1.0.0

@baseUrl = https://api.petstore.example.com/v1

###

# List all pets
# Returns a list of all pets in the store
# Operation ID: listPets

GET {{baseUrl}}/pets?limit=20&tag=example
```

### 6. Тестирование запросов (опционально)

Для тестирования сгенерированных запросов:

1. Установите расширение **REST Client** (humao.rest-client)
2. Откройте сгенерированный `.http` файл
3. Над каждым запросом появится кнопка **"Send Request"**
4. Кликните на нее для отправки запроса

### 7. Тестирование ошибок

Создайте файл `test-invalid.json` с содержимым:

```json
{
  "not": "openapi",
  "just": "random json"
}
```

Попробуйте сгенерировать .http файл из него - должно появиться сообщение об ошибке.

## Структура сгенерированных файлов

### Для GET запросов:

```http
GET {{baseUrl}}/path?param=value
```

### Для POST/PUT запросов:

```http
POST {{baseUrl}}/path
Content-Type: application/json

{
  "field": "value"
}
```

### С path параметрами:

```http
GET {{baseUrl}}/users/123
```

### С headers:

```http
GET {{baseUrl}}/path
X-Custom-Header: value
```

## Отладка расширения

### Логирование

В коде используется console.log/error. Просмотр в VS Code:

1. **View > Output**
2. Выберите **"Extension Host"** в выпадающем списке

### Точки останова

1. Откройте файл в `src/` (например, `extension.ts`)
2. Кликните слева от номера строки для установки breakpoint
3. Запустите расширение (F5)
4. Выполните действие - выполнение остановится на breakpoint

### Горячая перезагрузка

После изменения кода:

1. Сохраните файлы
2. В окне Extension Development Host: `Ctrl+R` или `Cmd+R`
3. Или нажмите кнопку перезагрузки в Debug toolbar

## Полезные команды

```bash
# Компиляция с отслеживанием изменений
npm run watch

# Проверка кода (линтинг)
npm run lint

# Сборка VSIX пакета
npm run package

# Очистка скомпилированных файлов
Remove-Item -Recurse -Force out
```

## Следующие шаги

1. ✅ Протестируйте на обоих примерах (YAML и JSON)
2. ✅ Попробуйте с невалидным OpenAPI файлом
3. ✅ Проверьте генерацию различных типов данных
4. ✅ Используйте REST Client для отправки запросов
5. 📝 Добавьте свои OpenAPI спецификации для тестирования

## Решение проблем

### Расширение не активируется

- Проверьте, что открыт JSON или YAML файл
- Убедитесь, что проект скомпилирован (`npm run compile`)
- Проверьте Output > Extension Host на наличие ошибок

### Не появляется пункт меню "Generate .http"

- Убедитесь, что активен редактор с JSON/YAML файлом
- Проверьте, что файл имеет правильное расширение (.json, .yaml, .yml)
- Перезапустите Extension Development Host (Ctrl+R)

### Ошибка при генерации

- Проверьте, что файл является валидным OpenAPI спецификацией
- Откройте Output > Extension Host для просмотра детальной ошибки
- Убедитесь, что OpenAPI версия 2.0 или 3.x

## Файлы для изучения

| Файл | Назначение |
|------|-----------|
| `src/extension.ts` | Регистрация команд и обработка UI |
| `src/openapi-parser.ts` | Парсинг и валидация OpenAPI |
| `src/http-generator.ts` | Генерация .http файлов |
| `src/data-generator.ts` | Генерация примеров данных |
| `package.json` | Манифест расширения |

## Документация

- [README.md](README.md) - Общая информация
- [USAGE.md](USAGE.md) - Подробное руководство пользователя
- [TESTING.md](TESTING.md) - Тестирование
- [CHANGELOG.md](CHANGELOG.md) - История версий

Удачной работы! 🚀
